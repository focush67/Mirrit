"use client";

import React from "react";

interface unFollowButtonProps {}

const unFollowButton = ({}: unFollowButtonProps) => {
  return <div>FollowButton</div>;
};

export default unFollowButton;
